apiVersion: v1
kind: Service
metadata:
  name: devops-kubernetes-app-service
  labels:
    app: devops-kubernetes-app
spec:
  selector:
    app: kubernetes-demo-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
